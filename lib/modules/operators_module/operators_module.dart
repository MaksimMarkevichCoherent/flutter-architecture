import 'package:flutter/material.dart';

class OperatorsModule extends StatelessWidget {
  const OperatorsModule({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
