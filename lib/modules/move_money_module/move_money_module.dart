import 'package:flutter/material.dart';

class MoveMoneyModule extends StatelessWidget {
  const MoveMoneyModule({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
